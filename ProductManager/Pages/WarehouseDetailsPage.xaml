<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:models="clr-namespace:ProductManager.DTOModels.Product;assembly=ProductManager.DTOModels"
             xmlns:vms="clr-namespace:ProductManager.Viewmodels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="ProductManager.Pages.WarehouseDetailsPage"
             x:DataType="vms:WarehouseDetailsViewModel"
             x:Name="DetailsPage"
             Title="Warehouse Details" IsEnabled="{Binding IsNotBusy}">
    <ContentPage.Behaviors>
        <toolkit:EventToCommandBehavior
            EventName="Appearing"
            Command="{Binding BindingContext.RefreshDataCommand, Source={x:Reference DetailsPage}}" />
    </ContentPage.Behaviors>
    <Grid RowDefinitions="Auto,Auto,5*,Auto">
        <Border 
            Grid.Row="0"
            Padding="10" 
            Margin="0,0"
            StrokeThickness="5"
            Stroke="BlueViolet"
            BackgroundColor="BlueViolet">
            <VerticalStackLayout>
                <Label
                    Text="{Binding CurrentWarehouse.Name}"
                    Style="{StaticResource SubHeadline}"
                    TextColor="White">
                </Label>
                <Label 
                    HorizontalOptions="Center"
                    TextColor="White">
                    <Label.Text>
                        <MultiBinding
                            StringFormat="{}Located in {0}, Products: {1}, Total: ${2}">
                            <Binding Path="CurrentWarehouse.Location"
                                     Converter="{StaticResource EnumToDisplayName}">
                            </Binding>
                            <Binding Path="CurrentWarehouse.ProductCount"></Binding>
                            <Binding Path="CurrentWarehouse.TotalCost"></Binding>
                        </MultiBinding>
                    </Label.Text>
                </Label>
            </VerticalStackLayout>
        </Border>
        <Border Grid.Row="1"
                StrokeThickness="0">
            <Grid ColumnDefinitions="*, *">
                <SearchBar Grid.Column="0"
                    Margin="5"
                    Text="{Binding SearchQuery}"
                    Placeholder="Enter Warehouse Name..."
                    CancelButtonColor="BlueViolet"/>

                <HorizontalStackLayout Grid.Column="1" Margin="5" Spacing="10" VerticalOptions="Center" HorizontalOptions="Center">
                    <Label Text="Sort By:"  TextColor="White"
                           VerticalOptions="Center" HorizontalOptions="Center"/>
                    <Picker ItemsSource="{Binding SortOptions}"
                        SelectedItem="{Binding SelectedSortOption}"
                        VerticalOptions="Center" HorizontalOptions="Center"/>
                </HorizontalStackLayout>
            </Grid>
        </Border>
        <Border
            Grid.Row="2"
            Padding="0"
            Margin="0,0">
            <CollectionView
                ItemsSource="{Binding VisibleProducts}"
                SelectionMode="None"
                >
                <CollectionView.ItemTemplate>
                    <DataTemplate
                        x:DataType="models:ProductListDTO">
                        <SwipeView>
                            <SwipeView.RightItems>
                                <SwipeItems
                                    Mode="Reveal">
                                    <SwipeItem
                                        Text="Delete"
                                        BackgroundColor="Red"
                                        Command="{Binding Source={RelativeSource AncestorType={x:Type vms:WarehouseDetailsViewModel}}, Path=DeleteProductCommand}" 
                                        CommandParameter="{Binding .}"/>
                                </SwipeItems>
                            </SwipeView.RightItems>
                            <Border
                                Padding="10">
                            <Border.GestureRecognizers>
                                <TapGestureRecognizer
                                    Command="{Binding Source={RelativeSource AncestorType={x:Type vms:WarehouseDetailsViewModel}}, Path=LoadProductCommand}"
                                    CommandParameter="{Binding Id}"/>
                            </Border.GestureRecognizers>
                                <FlyoutBase.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem Text="Delete" 
                                            Command="{Binding DeleteProductCommand, Source={RelativeSource AncestorType={x:Type vms:WarehouseDetailsViewModel}}}"
                                            CommandParameter="{Binding .}"/>
                                    </MenuFlyout>
                                </FlyoutBase.ContextFlyout>
                                <VerticalStackLayout>
                                <Label
                                    Style="{StaticResource SubHeadline}"
                                    Text="{Binding Name}"
                                    HorizontalOptions="Center"/>
                                <Label
                                    HorizontalOptions="Center">
                                    <Label.Text>
                                        <MultiBinding
                                            StringFormat="{}Category: {0}, Price: ${1}">
                                            <Binding Path="Category" Converter="{StaticResource EnumToDisplayName}"/>
                                            <Binding Path="Price"/>
                                        </MultiBinding>
                                    </Label.Text>
                                </Label>
                            </VerticalStackLayout>
                            </Border>
                        </SwipeView>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>
        </Border>
        <Border
            Grid.Row="3"
            Padding="10"
            BackgroundColor="BlueViolet"
            StrokeThickness="5"
            Stroke="BlueViolet">
            <Border.GestureRecognizers>
                <TapGestureRecognizer
                    Command="{Binding Source={RelativeSource AncestorType={x:Type vms:WarehouseDetailsViewModel}}, Path=CreateClickedCommand}"/>
            </Border.GestureRecognizers>
            <Label
                Text="Add Product"
                TextColor="White"
                Style="{StaticResource SubHeadline}"
                />
        </Border>
        <BoxView
            Grid.RowSpan="3"
            IsVisible="{Binding IsBusy}"
            BackgroundColor="Gray"
            Opacity="0.2"
            InputTransparent="False"/>
        <ActivityIndicator
            Grid.RowSpan="3"
            Color="BlueViolet"
            IsRunning="{Binding IsBusy}"
            IsVisible="{Binding IsBusy}"
            VerticalOptions="Center"
            HorizontalOptions="Center"/>
    </Grid>
</ContentPage>